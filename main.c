#include "ft_printf.h"
#include <stdio.h>

int main(void)
{
    ft_printf("Yo le s\n");                        // Affiche la string de base
    ft_printf("Carac %c\n", 'B');          // Affiche la string + %c
/* ft_printf("String : %s\n", "Basile");         // Affiche la string + %s
    ft_printf("Nb : %d\n", 42);     */         // Affiche la string + %d
}